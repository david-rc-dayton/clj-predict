(ns clj-predict.core
  (:require [clj-predict.coordinates :as coord]
            [clj-predict.propagation :as prop]
            [clj-predict.solar :as sol])
  (:gen-class))

(defn -main
  []
  (println "Hello, World!"))
